interface CanvasRenderingContext2D {
  drawUnits (units: any, x: number, y: number): void;
  roundRect (sx: number, sy: number, ex: number, ey: number, r: number): void;
  shadow (sx: number, sy: number, ex: number, ey: number, r: number): void;
}
